apiVersion: v1
kind: Service
metadata:
  name: capstone-app
  namespace: dev
  labels:
    app: capstone-app
    environment: dev
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: capstone-app
